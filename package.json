{
  "name": "root",
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@voiceflow/commitlint-config": "1.1.1",
    "@voiceflow/eslint-config": "3.5.0",
    "@voiceflow/git-branch-check": "1.2.1",
    "@voiceflow/prettier-config": "1.0.6",
    "@voiceflow/tsconfig": "1.2.5",
    "@zerollup/ts-transform-paths": "^1.7.18",
    "commitizen": "^4.2.3",
    "cz-conventional-changelog": "^3.2.0",
    "depcheck": "^1.4.1",
    "eslint": "^7.4.0",
    "eslint-output": "^3.0.1",
    "fixpack": "^4.0.0",
    "husky": "^4.3.8",
    "lerna": "^4.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.0",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "ttypescript": "^1.5.12",
    "typescript": "^4.2.4"
  },
  "prettier": "@voiceflow/prettier-config",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build --concurrency=4 --stream",
    "clean": "yarn clean:artifacts && yarn clean:root",
    "clean:artifacts": "lerna run clean --parallel",
    "clean:root": "rimraf node_modules",
    "commit": "cz",
    "lint": "lerna run lint --concurrency=9 --parallel",
    "lint:fix": "lerna run lint:fix --concurrency=9 --parallel",
    "lint:quiet": "lerna run lint:quiet --concurrency=9 --parallel",
    "lint:report": "lerna run lint:report --concurrency=9 --parallel",
    "postinstall": "yarn bootstrap",
    "test": "lerna run test --concurrency=9 --parallel",
    "test:dependencies": "lerna run test:dependencies --concurrency=9 --parallel",
    "test:integration": "lerna run test:integration --concurrency=9 --parallel",
    "test:run": "lerna run test:run --concurrency=9 --parallel",
    "test:unit": "lerna run test:unit --concurrency=9 --parallel"
  },
  "workspaces": [
    "packages/*"
  ]
}
