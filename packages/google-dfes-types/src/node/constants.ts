import { Models } from '@voiceflow/base-types';

export type BaseCommand = Models.BaseCommand;
export type BaseNode = Models.BaseNode;
export type BaseStep = Models.BaseStep;

export enum NodeType {
  PAYLOAD = 'payload',
}
